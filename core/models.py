# Core models for Phase 1
# This file contains minimal core models for the basic platform functionality

from django.db import models

# For now, we rely on:
# - Django's built-in User model (customized in apps.users)
# - Django's session framework
# - Basic Django admin functionality
